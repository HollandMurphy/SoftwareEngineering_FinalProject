function checkInventory(Make, Year, Model, Trim, StockNumber, VinNumber, Class, Age, Body, Color, Disp, Cost, Odometer){

//unit testing stub to check months of supply
        var returnStatus = "baddata";

        returnStatus = "gooddata";

        return returnStatus;
}
